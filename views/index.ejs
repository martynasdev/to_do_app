<!DOCTYPE html>
<html>

<head>
    <title>ToDo</title>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link rel="stylesheet" type="text/css" media="screen" href="/assets/main.css" />
    <link href="https://fonts.googleapis.com/css?family=Montserrat" rel="stylesheet">
</head>

<body>
    <div class="appToDo">
        <div class="appTitle">
            <h1>What do you have to do?</h1>
        </div>
        <div class="appInput">
            <form method="POST" action="/index">
                <input class="inputTextArea" type="text" name="appInputText" placeholder="Write task here..." autocomplete="off">
                <button type="submit" value="prideti">+</button>
            </form>
        </div>
        <div class="appContentList">
            <h2>
                <% for(let i = 0; i < appInputText.length; i++){ %>
                <div class="taskP" href="/edit/<%= i %>">
                        <%= appInputText[i].appInputText %>
                </div>
                <% } %>
            </h2>
        </div>
    </div>
</body>

</html>